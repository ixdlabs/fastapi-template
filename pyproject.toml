[project]
name = "sample-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.124.2",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "ruff>=0.14.8",
]
